# Intentional fake secrets for the VHS demo recording
assets/demo.tape

# Test files contain intentional fake secrets for detection validation.
# Wick is a secret-detection tool, so its tests necessarily embed sample
# credentials. Ignore all Go test files rather than enumerate individual
# paths — enumerated internal/ paths silently went stale in the v0.2 refactor
# when the packages were promoted out of internal/.
*_test.go
