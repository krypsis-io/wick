name: Scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 2 * * 0"

permissions:
  contents: read
  security-events: write
  id-token: write

jobs:
  scorecard:
    uses: krypsis-io/.github/.github/workflows/scorecard.yml@main
